# 知惑 Zhihuo · 页面设计优化清单

> 视角：视觉 / UI 设计
> 日期：2026-06-08
> 关系：承接《PRODUCT_OPTIMIZATION.md》（第一阶段：前端手感）。本篇是第二阶段，只谈"页面长什么样"，不重复交互手感。
> 原则：安静、克制、统一。设计上的优化不是加东西，是**把已经存在的东西收齐**。

---

## 一个总判断

这套界面的**质感底子很好**——纸纹、星点、节气、衬线字、低饱和暗色，氛围是对的，没有廉价感。

但它**精致在局部，松散在整体**。问题不在"不好看"，在于"每个页面像是不同时间、不同心情各自调出来的"，缺一套贯穿三层的尺子。下面全是"收口"的活，不是"重做"。

---

## P0 · 跨层一致性（最该先收）

### 1. 三层底色割裂，像三个 App

- 生活层：深黑（`#07090d`，`.life-surface`）
- 思路层：米白（`#f5f3f0`，`.thinking-surface`）
- 设置层：冷蓝（`#eef1f5`，`.settings-surface`）

从生活层（暗）切到思路层（亮）是一次**亮度暴击**，尤其夜间使用，瞳孔要重新适应。设置层又是另一支色相（蓝），和前两个的暖灰调不搭。

这不一定是错——"暗夜放疑问 / 白日理思路"可能是有意的隐喻。但目前**三者之间没有过渡**，切换是硬切。建议二选一：
- **方案 A（统一调性）**：三层共用一个暖灰色温，只调明度（暗→中→亮），把设置层的冷蓝拉回暖灰，让三层是"同一张纸的不同光照"。
- **方案 B（保留对比但做过渡）**：切层时加一层 200–300ms 的亮度渐变遮罩，不要硬切。

我倾向 A——更符合"安静、顺手"，少一次视觉成本。

### 2. 建立字号阶梯（现在是散的）

实测字号散落：`11px×27`、`12px×13`、`13/14/15px`、`16/17/23/28px`、`1.35rem/1.02rem/0.95rem`……**正文 14 还是 15 没有定论，小字 11 还是 12 没有定论。** 这是"松散感"最大的来源。

建议定一套 type scale（CSS 变量），全项目只允许用这几级：

```
--text-display: 1.5rem   (24) 标题
--text-title:   1.125rem (18) 区块标题
--text-body:    0.9375rem(15) 正文 / 输入
--text-meta:    0.8125rem(13) 次要信息
--text-caption: 0.6875rem(11) 标签 / 计数
```

把 `12px`、`14px`、`16px`、`17px` 这些中间值逐步并掉。**光是把正文统一到 15、小字统一到 11，整体就会立刻"齐"。**

### 3. 字间距收敛到 3 档

现在有 12 种 `tracking` 值（0.01 / 0.015 / 0.03 / 0.04 / 0.05 / 0.06 / 0.08 / 0.1 / 0.12 / 0.14 / 0.16 / 0.28em）。中文衬线本就需要字距，但 12 档是失控。收成：
- 正文：`0.03em`
- 标签/元信息：`0.08em`
- 大标题/仪式感文字：`0.16em`

`0.28em` 这种极端值只在极少数装饰标题保留。

---

## P1 · 节奏与留白

### 4. 间距也定阶梯（4 的倍数）

和字号同理，padding/gap 建议锁到 4px 基准：`4 / 8 / 12 / 16 / 24 / 32 / 48`。现在 `pt-16 pb-8`、`px-8 lg:px-12`、各种 `gap-1.5 / gap-3 / gap-4` 是混的。统一后竖向节奏会更稳。

### 5. 生活层 hero 占比偏重

`life-layer.tsx` 头部 `pt-16 pb-10` + 标题 + "N 缕思绪在此沉淀" + 输入区，首屏吃掉很大一块，疑问列表被压到下面。移动端尤其明显。建议压缩 hero 竖向间距，让"已有的疑问"更早进入视野——这也更"顺手"。

### 6. 笺的两种尺寸要对齐网格

`letter-paper.tsx`：standard 是 `aspect-[3/4] max-w-[480px]`，long 是 `min-h-[720px] max-w-[640px]`。两种笺的内边距、行高、标题位置建议共用同一组 token，避免长笺看起来像另一个模板。

---

## P2 · 细节质感

### 7. 焦点态统一

按钮 `focus-visible:ring-1 ring-ring`，但输入框普遍 `focus-visible:ring-0`，靠 `.time-input-shell:focus-within` 的微光提示，而思路层输入又是另一套（`thinking-layer.tsx` 白底 ring-0）。三种焦点语言。建议统一为"一条渐显的底线 + 极淡辉光"，三层共用一个 `:focus-within` 类。

### 8. 圆角统一

`--radius: 0.75rem` 已定义，但代码里 `rounded-full`、`rounded-md`、`rounded-[...]` 混用。规则化：
- 按钮/标签：`rounded-full`
- 卡片/面板/输入：`var(--radius)`
- 不要再出现裸 `rounded-md`（0.375rem）与 0.75rem 并存。

### 9. 描边太多近乎隐形的值

`--time-border: rgba(255,255,255,0.026)`、divider `0.04`、各种 `border-white/[0.03]`。低于 5% 的描边在多数屏幕上等于没有，却仍参与渲染。要么提到能看见（~8%），要么干脆去掉用间距分隔。**模糊的弱描边比没有描边更显脏。**

### 10. 朱印 / 节气 / 月相 是产品记忆点，可更"立"

笺上的印章（`letter-seal`）、`清明·初日`、月相 glyph 是这个产品独有的视觉资产。目前它们偏淡、偏装饰。建议在笺导出版本里让它们**对比度稍高一点**——这是用户会截图分享出去的画面，是产品的"脸"，值得比屏幕版更清晰半档。

---

## 落地顺序

1. **#1 三层色温 + #2 字号阶梯 + #3 字距** —— 这三条解决 80% 的"松散感"，且都是改 token / 全局替换，风险低。
2. #4 间距阶梯、#7 焦点统一、#8 圆角 —— 第二轮收口。
3. 其余细节按需。

这些做完，界面不会变成另一个样子，但会从"挺好看"变成"挺讲究"。要的话我先从 #2 字号阶梯落地，建一组 CSS 变量并替换最高频的几处。
