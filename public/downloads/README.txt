Place the published Android package here as:

  public/downloads/zhihuo-latest.apk

The /apk page will automatically expose this file when it exists.

Alternatively, set NEXT_PUBLIC_APK_DOWNLOAD_URL to point at an externally hosted APK.
